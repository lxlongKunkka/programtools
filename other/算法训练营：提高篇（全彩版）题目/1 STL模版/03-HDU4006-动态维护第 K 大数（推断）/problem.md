# HDU4006 动态维护第 K 大数（推断）

## 题目描述
给定一系列操作，支持向数据结构中插入一个整数，以及查询当前所有已插入整数中的第 $k$ 大值。

## 输入格式
输入包含多组数据。每组数据第一行输入两个整数 $n,k$，表示操作总数和查询的排名。

接下来有 $n$ 个操作，每个操作为以下两种之一：

- I x：插入一个整数 $x$；
- Q：查询当前第 $k$ 大的整数。

## 输出格式
对于每个查询操作，输出一行一个整数，表示当前第 $k$ 大值。

## 说明
该题面根据 AC 代码行为推断生成，代码使用大小为 $k$ 的小根堆维护答案，建议后续人工复核。
