# POJ1195 二维点修改矩形求和（推断）

## 题目描述
维护一个二维平面上的整数矩阵，支持单点增加和子矩形区域求和。

## 输入格式
输入由若干条操作组成：

- 0 n：初始化一个边长为 $n$ 的矩阵；
- 1 x y a：将位置 $(x,y)$ 的值增加 $a$；
- 2 l b r t：查询左下角到右上角矩形内元素和；
- 3：结束输入。

## 输出格式
对于每个类型 2 的操作，输出一行一个整数，表示查询结果。

## 说明
该题面根据 AC 代码行为推断生成，代码使用二维树状数组实现，建议后续人工复核。
