# POJ2823 滑动窗口最值（推断）

## 题目描述
给定一个长度为 $n$ 的整数序列和窗口大小 $k$，请分别求出每个长度为 $k$ 的连续子区间中的最小值和最大值。

## 输入格式
第一行输入两个整数 $n,k$。

第二行输入 $n$ 个整数，表示序列元素。

## 输出格式
输出两行：

- 第一行按顺序输出每个窗口中的最小值；
- 第二行按顺序输出每个窗口中的最大值。

同一行中的数字之间用空格分隔。

## 说明
该题面根据 AC 代码行为推断生成，默认代码使用单调队列线性求解，建议后续人工复核。
