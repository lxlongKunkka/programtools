# POJ1741 树上距离不超过 K 的点对数（推断）

## 题目描述
给定一棵带权树和整数 $k$，统计有多少对不同结点满足它们之间的距离不超过 $k$。

## 输入格式
输入包含多组数据。每组数据输入结点数 $n$ 和参数 $k$，随后输入 $n-1$ 条带权边。当 $n=0$ 且 $k=0$ 时结束。

## 输出格式
对于每组数据，输出一行一个整数，表示满足条件的点对数量。

## 说明
该题面根据 AC 代码行为推断生成，默认代码使用点分治统计树上路径，建议后续人工复核。
