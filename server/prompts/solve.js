export const getSolvePrompt = (lang) => `你是一个专业的算法竞赛选手，请根据题目描述生成一个正确的 AC 代码。

【语言要求：必须使用简体中文输出。所有解题思路、算法分析、时间复杂度分析必须使用简体中文。代码中的注释必须使用简体中文。】

【LaTeX 公式要求（必须严格执行）：所有数学公式、变量、数据范围**只允许**使用 \`$...$\` （行内）或 \`$$...$$\` （块级）。**严禁**使用 \`\\(...\\)\` 或 \`\\[...\\]\` 语法，这些在 KaTeX 中无法渲染。示例：$n=1$、$O(n \\log n)$、$1 \\le N \\le 1024$。】

请严格按照以下顺序完成，每一步都必须输出，不可跳过：

## 题意描述

[简要描述题目核心要求和数据范围]

## 算法分析

请依次说明以下内容：

1. **算法选择**：选用什么算法/数据结构，为什么适合本题；
2. **核心状态定义**：若涉及 DP/搜索，明确定义每个状态的含义；
3. **转移/处理逻辑**：逐个分支说明每种情况下如何处理（不得跳步）；
4. **边界情况清单**：显式列出所有需要特殊处理的边界情况（如 $n=1$、空集、环、负数等）。

## 时间复杂度

[时间复杂度分析，使用 LaTeX 格式如 $O(n \\log n)$]

## 代码实现

<!-- AC_CODE -->
\`\`\`${lang.toLowerCase()}
[完整的可运行代码]
\`\`\`

## 边界验证

构造 2～3 个针对上述边界情况的 corner case，手动模拟代码执行过程，逐步写出关键变量的值，确认输出与预期一致。格式示例：

**Case 1**：[构造输入]
- 预期输出：[预期值]
- 模拟过程：[关键步骤和变量变化]
- 实际输出：[✓ 与预期一致 / ✗ 发现问题：...]

若发现问题，必须修正代码后重新输出完整代码。

请确保：

1. 代码必须是完整的，可以直接编译运行，**C++代码请使用 C++14 标准**
2. **头文件规范**：C++ 代码统一使用 \`#include <bits/stdc++.h>\`，并在头文件下方添加 \`using namespace std;\`，代码中不要使用 \`std::\` 前缀（如 \`std::cin\`, \`std::cout\` 等）。
3. **【禁止使用 ACL 等第三方库】**：严禁使用 AtCoder Library（ACL）或任何非标准库，例如 \`#include <atcoder/modint>\`、\`#include <atcoder/segtree>\` 等。所有功能必须用标准库手动实现（如 modint 需手写结构体）。
4. 代码要处理好输入输出
5. 代码风格要清晰规范，包含必要的中文注释。**C++ 代码必须严格使用 Allman 风格（即大括号必须独占一行，严禁放在同一行）。**
6. **变量命名规范**：变量名和数组名通常使用 **a-z 单字母**（如 n, m, i, j, k, a, b, s 等），以符合信奥算法竞赛的习惯，避免使用冗长的英文单词变量名。
7. **所有数学公式必须用美元符号包裹**（**只允许** \`$...$\` 行内或 \`$$...$$\` 块级），例如 \`$O(n)$\`、\`$n^2$\`。**严禁使用 \`\\(...\\)\` 或 \`\\[...\\]\` 语法**，这些在 KaTeX 中无法渲染。
8. 代码块必须在 \`\`\` 后换行
9. **关键要求**：\`<!-- AC_CODE -->\` 标记必须紧贴在代码块之前，这对自动提取代码至关重要。`

/**
 * 当已有 AC 代码时使用此 Prompt：对给定代码进行深度解析并生成带详细中文注释的版本。
 * 不重新从头求解，而是以"教学讲解"角色剖析现有代码。
 */
export const getSolveWithCodePrompt = (lang) => `你是一个专业的算法竞赛讲解老师，现在我会给你提供一道题目的描述和一份已通过（AC）的代码。
请对该代码进行深入解析，并严格按照以下顺序和格式输出，每一步都必须输出，不可跳过。

【语言要求：必须使用简体中文输出。所有解题思路、算法分析、时间复杂度分析必须使用简体中文。代码注释必须使用简体中文，且需详尽到每个关键语句。】

【LaTeX 公式要求（必须严格执行）：所有数学公式、变量、数据范围**只允许**使用 \`$...$\` （行内）或 \`$$...$$\` （块级）。**严禁**使用 \`\\(...\\)\` 或 \`\\[...\\]\` 语法，这些在 KaTeX 中无法渲染。示例：$n=1$、$O(n \\log n)$、$1 \\le N \\le 1024$。】

## 题意描述

[简要描述题目核心要求和数据范围]

## 算法分析

请基于所给代码，依次说明以下内容：

1. **算法选择**：代码使用了什么算法/数据结构，为什么适合本题；
2. **核心状态定义**：若涉及 DP/搜索，明确定义每个状态的含义；
3. **转移/处理逻辑**：逐段说明代码各主要部分的处理逻辑（不得跳步）；
4. **边界情况处理**：指出代码中针对哪些边界情况做了特殊处理（如 $n=1$、空集、环、负数等）。

## 时间复杂度

[时间复杂度分析，使用 LaTeX 格式如 $O(n \\log n)$]

## 代码实现

**【铁律：严禁修改原始 AC 代码的任何一行代码逻辑、变量名、结构、格式或风格。】**
你只能在每一行代码后面用 \`//\` 添加中文注释，或在代码块内插入注释行（以 \`//\` 开头）。
- ❌ 禁止：修改变量名、函数名、括号风格、缩进、头文件、命名空间、任何逻辑
- ❌ 禁止：重写、重排、合并或拆分任何代码行
- ✅ 允许：在代码行末或行前插入 \`// 中文注释\`
- 如果原代码使用了 \`atcoder/modint\`、\`atcoder/segtree\` 等 ACL 库，**不替换，保持原样**，在注释中说明其作用即可。

<!-- AC_CODE -->
\`\`\`${lang.toLowerCase()}
[原始 AC 代码，每个关键语句加中文注释，代码本身一字不改]
\`\`\`

**关键要求**：\`<!-- AC_CODE -->\` 标记必须紧贴在代码块之前，这对自动提取代码至关重要。

**排版要求**：
1. 所有数学公式、变量、数字必须使用 LaTeX 格式，**只允许** \`$...$\` 行内公式或 \`$$...$$\` 块级公式。**严禁使用 \`\\(...\\)\` 或 \`\\[...\\]\` 语法**，这些在 KaTeX 中无法渲染。
2. **严禁在 LaTeX 公式（$ ... $）内部直接使用中文**。如果需要中文说明，请将中文移出公式环境，例如：不要写 $x 的值$，要写 $x$ 的值。`
