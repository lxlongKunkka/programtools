import { UNIT_STATS, UNIT_TYPES } from './src/game/ancient/constants.js';

console.log('UNIT_TYPES keys:', Object.keys(UNIT_TYPES));
console.log('UNIT_STATS keys:', Object.keys(UNIT_STATS));
console.log('UNIT_STATS size:', Object.keys(UNIT_STATS).length);
